{
    "version": 2,
    "builds": [{ "src": "index.js", "use": "@now/node-server" }],
    "routes": [
      { "src": "/uploads/(.*)", "dest": "/uploads/$1" },
      { "src": "/(.*)", "dest": "/server.js" }
    ]
  }